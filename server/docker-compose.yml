# Local development / testing — just the app, no nginx
services:
  app:
    build: .
    restart: unless-stopped
    environment:
      - PORT=3001
      - LOG_LEVEL=${LOG_LEVEL:-debug}
    ports:
      - "3001:3001"
    volumes:
      - ./translations:/app/translations
      - ./versions:/app/versions
