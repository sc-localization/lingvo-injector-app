name: PR Check

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - 'docs/**'
      - '**.md'

jobs:
  check-changes:
    name: Check Changes
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    outputs:
      app: ${{ steps.filter.outputs.app }}
      server: ${{ steps.filter.outputs.server }}
      ci: ${{ steps.filter.outputs.ci }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check for file changes
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            app:
              - 'app/**'
              - 'package.json'
              - 'package-lock.json'
            server:
              - 'server/**'
            ci:
              - '.github/**'

  lint:
    name: Lint
    needs: check-changes
    runs-on: ubuntu-latest
    if: ${{ needs.check-changes.outputs.app == 'true' || needs.check-changes.outputs.server == 'true' }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Dependencies
        uses: ./.github/actions/setup-dependencies
        with:
          node-version: '20'
          platform: ubuntu-latest

      - name: Run ESLint
        run: npm run lint

  build-app:
    name: Build App
    environment: pr-review
    needs: [check-changes, lint]
    if: ${{ needs.check-changes.outputs.app == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        platform: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Dependencies
        uses: ./.github/actions/setup-dependencies
        with:
          node-version: '20'
          platform: ${{ matrix.platform }}

      - name: Build Tauri App
        run: npm run tauri:build:dev --workspace=app

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ runner.os }}-build
          path: app/src-tauri/target/release/bundle/
          compression-level: 6
          if-no-files-found: warn

  status-check:
    name: PR Status
    needs: [check-changes, lint, build-app]
    runs-on: ubuntu-latest
    if: ${{ always() }}
    steps:
      - name: Check Status
        run: |
          echo "App changes: ${{ needs.check-changes.outputs.app }}"
          echo "Server changes: ${{ needs.check-changes.outputs.server }}"
          echo "Lint result: ${{ needs.lint.result }}"
          echo "Build result: ${{ needs.build-app.result }}"

          # If no code changes, pass
          if [ "${{ needs.check-changes.outputs.app }}" != "true" ] && [ "${{ needs.check-changes.outputs.server }}" != "true" ]; then
            echo "No code changes, passing"
            exit 0
          fi

          # Check lint
          if [ "${{ needs.lint.result }}" != "success" ] && [ "${{ needs.lint.result }}" != "skipped" ]; then
            echo "Lint failed"
            exit 1
          fi

          # Check build if app changed
          if [ "${{ needs.check-changes.outputs.app }}" == "true" ]; then
            if [ "${{ needs.build-app.result }}" != "success" ] && [ "${{ needs.build-app.result }}" != "skipped" ]; then
              echo "Build failed"
              exit 1
            fi
          fi

          echo "All checks passed"
